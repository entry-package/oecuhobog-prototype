# 大阪電気通信大学高等学校同窓会 クライアント確認用サイト

8月26日版のデザインを土台に、2026年9月10日に取得した公開記事86件・公開コメント6件・画像資料571参照を統合しています。

確認URL: https://entry-package.github.io/oecuhobog-prototype/

記事検索、画像、資料、既存のコメントを確認できます。確認用のためフォーム送信は停止しています。現行の独自ドメインやStrikinglyは変更していません。

公開するのは確認済みの静的サイトだけです。管理画面の非公開データ、保存データベース、テスト送信は含めていません。検索エンジン向けにはnoindexを指定しています。

## 2026-09-14 監査修正

旧フッター17ページ、会長挨拶、固定記事、記事カバー、RSS、SNS情報、終了済み募金の案内を修正。詳細は `migration-review/audit-fixes-20260914.md` を参照。

記事を再生成した後は、公開ソースのスナップショットを指定して `python3 tools/apply_audit_fixes.py --site-root . --source-root ../source` を再適用する。Python 3 と Beautiful Soup 4 が必要。`--origin` は配置先のホスト、`--prefix` は配置先のパスを指定する。全HTMLのRSS・SNSメタ情報は配置先と合わせる。本文の既存リンクのルート変換は別途必要。

今回の修正はローカル検証済み・未公開。フォームの本番保存・管理・通知、読者メール配信、GA4・Cookie同意、非公開データの退避、独自ドメイン切替は未完了。
