# 同窓会サイトの移行監査修正（2026-09-14）

旧フッター17ページ、会長挨拶、固定記事、記事カバー、RSS、SNS情報、終了済み募金の案内を修正した。記事本文と公開コメント、画像資料を保持する。

## 確認結果

- 121 HTMLの内部ファイル参照に欠落なし。
- 記事本文86件、公開コメント6件、旧ページ本文18件は意図した旧フッター除去以外を保全。
- 画像・資料576ファイルは変更前とバイト一致。
- 会長挨拶の全文は元の公開本文と一致。
- RSSは86件、XML解析成功、GUID重複なし。旧URLとXML版は同じ内容。
- PCの会長挨拶と、390px幅の会長挨拶・写真付き記事を確認。記事検索は一致5件、該当なし0件で表示確認。
- 再適用時の差分なし。noindexとフォーム送信停止を保持。

## 再適用

Python 3とBeautiful Soup 4を用い、記事生成後に仕上げ処理を実行する。

```sh
python3 tools/apply_audit_fixes.py --site-root . --source-root ../source
```

公開ソースのスナップショットはリポジトリ外に置く。`--origin`と`--prefix`でRSS・SNS情報の配置先を指定する。本文の既存ルートは別工程で変換する。本番用は配置先・HTTPのContent-Type・旧URLを実環境で検証する。

## 運用の残件

本修正はローカル確認用で未公開。フォームの本番保存・管理・通知、コメント承認、読者へのメール配信、管理画面、GA4とCookie同意、独自ドメイン切替は未完了。非公開データは公開物に含めない。

全ページの全幅目視、実スマートフォン、SNSサービスの実カード表示、公開後のRSSクライアント互換性は未検証。

戻し先: `2ecf7b6`。
